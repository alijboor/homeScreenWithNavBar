enum ELayout {
  ///To build operations view inside [GridView] default cross 2
  grid,

  ///To build operations view inside scrollable row - horizontal scroll
  row,

  ///To build operations view inside scrollable column - vertical scroll
  column;
}